name: Notificar Igor no PR (Alternativa Log)

on:
  pull_request:
    types: [opened]

# Adicionamos as permissões para o caso de querer usá-las mais tarde
permissions:
  issues: write 
  pull-requests: write 
  contents: read

jobs:
  notificar_colega:
    runs-on: ubuntu-latest
    steps:
      - name: Notificar Colega via Log
        # Este comando escreve diretamente no log do Action
        run: |
          echo "::notice::👋 Olá, @IGOR-222222! O Pull Request #${{ github.event.number }} foi aberto. Por favor, revise o Javadoc que documentei."
          echo "Aviso: O PR de Javadoc requer a sua atenção!"
          
      - name: Mensagem no Resumo do Workflow
        # Adiciona a mensagem visível na página de resumo do Action
        run: echo "## 📢 Revisão Solicitada: @IGOR-222222" >> $GITHUB_STEP_SUMMARY
