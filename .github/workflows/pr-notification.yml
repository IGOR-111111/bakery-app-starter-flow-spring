name: Notificar Autor do PR

# Este workflow é executado quando um Pull Request é aberto ou reaberto
on:
  pull_request:
    types: [opened, reopened]

jobs:
  comment_on_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Adicionar Comentário ao PR
        # Usamos uma Action de terceiros (peter-evans/create-or-update-comment)
        # que facilita a criação de comentários em Issues ou PRs.
        uses: peter-evans/create-or-update-comment@v4
        with:
          # O número da Issue/PR onde o comentário será feito.
          issue-number: ${{ github.event.pull_request.number }}
          # A mensagem que será o corpo do comentário.
          body: |
            👋 Olá, @${{ github.event.pull_request.user.login }}!
            
            Obrigado por abrir este Pull Request! A nossa CI/CD começou a correr as verificações.
            
            **Tarefas importantes:**
            1. Confirme que todos os requisitos do PR foram cumpridos.
            2. Peça um *review* quando estiver pronto.
          # O token de autenticação (fornecido automaticamente pelo GitHub)
          token: ${{ secrets.GITHUB_TOKEN }}
