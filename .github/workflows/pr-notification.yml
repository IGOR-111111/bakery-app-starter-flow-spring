name: Notificar Igor para Revisão (Alternativa)

on:
  pull_request:
    types: [opened]

jobs:
  notificar_igor:
    runs-on: ubuntu-latest
    
    # 💥 PERMISSÃO CRÍTICA (Adicione ao seu YAML) 💥
    permissions:
      issues: write
      contents: read
      
    steps:
      - name: Adicionar Comentário ao PR e Notificar Igor
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = context.issue.number;
            const prAuthor = context.payload.pull_request.user.login;
            
            const message = `
              **⚠️ Atenção, @Igor-222222!**
              
              Um novo Pull Request (#${prNumber}) foi aberto por **@${prAuthor}**.
              
              É necessária a sua revisão deste PR.
            `;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: message
            });
