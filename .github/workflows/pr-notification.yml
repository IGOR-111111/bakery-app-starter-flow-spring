name: Notificar Igor no PR

on:
  pull_request:
    types: [opened] # Executa este workflow apenas quando um PR é aberto

jobs:
  notificar_colega:
    runs-on: ubuntu-latest
    steps:
      - name: Adicionar Comentário de Notificação ao Colega
        # Usa uma Ação de terceiros para comentar no PR
        uses: actions/github-script@v6
        with:
          script: |
            // O contexto do GitHub fornece todas as informações do evento, 
            // incluindo o número do PR (issue_number) e o repositório.
            
            // Variável com o nome de utilizador do colega
            const reviewer = 'IGOR-222222';
            
            // Envia um comentário para notificar o colega
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `👋 Olá, @${reviewer}! O Pull Request #${context.issue.number} foi aberto. Por favor, revise o Javadoc que documentei.`,
            });
