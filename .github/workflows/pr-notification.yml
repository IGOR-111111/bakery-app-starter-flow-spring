name: Notificar Igor no PR

on:
  pull_request:
    types: [opened]

# 🚨 A correção CRÍTICA para o erro 403 Forbidden
permissions:
  contents: read # Permissão padrão para ler o código
  issues: write # Permite que o Action crie comentários (em Issues e PRs)

jobs:
  notificar_colega:
    # Garante que o token de segurança no job tenha as permissões definidas acima.
    runs-on: ubuntu-latest
    steps:
      - name: Adicionar Comentário de Notificação ao Colega
        # Usa a variável GITHUB_TOKEN que agora tem permissão de issues: write
        uses: actions/github-script@v6
        with:
          script: |
            const reviewer = 'IGOR-222222';
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `👋 Olá, @${reviewer}! O Pull Request #${context.issue.number} foi aberto. Por favor, revise o Javadoc que documentei.`,
            });
